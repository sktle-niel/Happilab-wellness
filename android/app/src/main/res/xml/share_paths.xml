<?xml version="1.0" encoding="utf-8"?>
<!-- What the share FileProvider may hand out: only the app's cache, which is
     where Dart writes the one temporary picture a share attaches. -->
<paths>
    <cache-path name="shared" path="." />
</paths>
